int ObtCantidadMinusculas(char cadena[])
{
	int cantidad = 0;

	for(int i =0; i < strlen(cadena); i++)
	{
		if( (cadena[i] >= 'a') && (cadena[i] <= 'z') )
		{
			cantidad++;
		}
	}

	return cantidad;
}
